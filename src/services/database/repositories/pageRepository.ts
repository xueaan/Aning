import type { Page } from '@/types';

export class PageRepository {
  // Placeholder for page repository methods
  static async findAll(): Promise<Page[]> {
    return [];
  }

  static async findById(_id: string): Promise<Page | null> {
    return null;
  }
}
